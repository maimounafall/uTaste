<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:orientation="vertical"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:background="@drawable/dialog_background"
    android:padding="20dp"
    android:clipToOutline="true"
    android:elevation="8dp">

    <!-- Title -->
    <TextView
        android:id="@+id/tvModifyIngredientTitle"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:text="Modify Ingredient Quantity"
        android:textAlignment="center"
        android:textColor="@color/burgundy"
        android:textStyle="bold"
        android:textSize="20sp"
        android:layout_marginBottom="20dp" />

    <!-- Ingredient name (non-editable) -->
    <TextView
        android:id="@+id/tvNumberOfSales"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_marginBottom="12dp"
        android:gravity="center_horizontal"
        android:text="Ingredient: Flour"
        android:textColor="@color/burgundy"
        android:textSize="22sp"
        android:textStyle="bold" />

    <!-- Quantity input -->
    <EditText
        android:id="@+id/etIngredientQuantity"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:hint="Enter new quantity (%)"
        android:inputType="numberDecimal"
        android:background="@drawable/rounded_input"
        android:padding="10dp"
        android:textColor="@color/black"
        android:textColorHint="#888"
        android:layout_marginBottom="20dp" />

    <!-- Buttons -->
    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="horizontal"
        android:gravity="center">

        <!-- Cancel -->
        <Button
            android:id="@+id/btnCancel"
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:layout_marginEnd="8dp"
            android:layout_weight="1"
            android:background="@drawable/pale_background"
            android:paddingHorizontal="16dp"
            android:paddingVertical="10dp"
            android:text="Cancel"
            android:textColor="@color/pale_grey"
            android:textSize="15sp"
            android:textStyle="bold" />

        <!-- Save -->
        <Button
            android:id="@+id/btnSave"
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:layout_weight="1"
            android:background="@drawable/rounded_button"
            android:paddingHorizontal="16dp"
            android:paddingVertical="10dp"
            android:text="Save"
            android:textColor="@color/burgundy"
            android:textSize="15sp"
            android:textStyle="bold" />
    </LinearLayout>
</LinearLayout>
